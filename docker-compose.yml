services:
  app:
    build: .
    container_name: api-gateway-app
    ports:
      - "8080:8080"
    env_file:
      - .env
    networks:
      - network-RedNorte

networks:
  network-RedNorte:
    external: true
